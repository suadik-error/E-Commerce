services:
  - type: web
    name: suad-busi-tech-api
    env: node
buildCommand: cd backend && npm install
startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
    autoDeploy: true
